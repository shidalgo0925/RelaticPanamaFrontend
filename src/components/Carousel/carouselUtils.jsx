// Funciones auxiliares para el componente Carousel

/**
 * Función para dividir el título en dos partes con estilo de tipografía
 * @param {string} title - El título a renderizar
 * @returns {JSX.Element} - Elemento JSX con el título estilizado
 */
export const renderStyledTitle = (title) => {
  return <span className="text-white">{title}</span>;
};

